overwrite: true
schema: "schema/**/*.graphql"
documents: null
generates:
  src/__generated__/typedefs.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
  ./graphql.schema.json:
    plugins:
      - "introspection"
